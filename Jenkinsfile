@Library('my-shared-lib') _

// Using the template defined in the library
standardPipeline(
    appName: 'DemoApp',
    user: 'DevOps Learner',
  //  agentLabel: 'slave-node' // Ensure your slave container has this label
)
